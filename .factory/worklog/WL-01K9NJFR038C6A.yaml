id: WL-01K9NJFR038C6A
at: 2026-08-28
by: claude
buoc: s8 — FR-038/C6a hai màn nạp riêng, bỏ màn `/nap/` chung
loai: bàn giao C6a
fr: FR-038-tach-ba-module-noi-dung.md

object:
  task:
    - 07_plan/M01_core/tasks/T01-28-khai-hai-man-nap-rieng.md   # code
    - 07_plan/M03_web/tasks/T03-30-hai-man-nap-rieng.md         # code
    - 07_plan/M03_web/tasks/T03-31-test-man-nap-rieng.md        # test
  code:
    - core/assets/man-hinh.json     # +nap-bai-viet +nap-tai-lieu, −nap
    - web/render/shell.html         # DỜI panes; dải tab còn 3; #mbnaptl riêng
    - web/plugins/home-pages/shell.html   # byte-identical
    - web/render/trang.mjs          # catNap tổng quát · MAN_LOAI siết · #mbnaptl
    - web/plugins/multiwindow/src/scripts/multiwindow.inline.ts  # gỡ moTabNap
    - web/plugins/WORKLOG.md · web/WORKLOG.md
  test:
    - web/test/man-nap-rieng.test.js   # MỚI — 4 mục
    - web/test/_render.mjs · ssr-routes · cac-man-con-lai · four-screens
    - web/test/khung-8-o · thu-vien-nap · chu-giao-dien · bay-man
    - web/test/WORKLOG.md · web/package.json

may_xanh:
  - "cd web && node test/man-nap-rieng.test.js  ->  pass (4 mục)"
  - "cd web && npm test  ->  exit 0 (57 file)"
  - "PYTHONIOENCODING=utf-8 pytest core/tests -q  ->  42 passed"
  - "18/22 cổng check_*.py — bốn đỏ y hệt WL-01K9NGFR039, không thêm đỏ mới"
  - >
    ngân sách: gn.css 92/100 · gn.js 95/100 · trang chủ 46/60 · trang lớn nhất
    **55**/74 (giảm từ 57 — mỗi trang thôi mang màn nạp không phải của nó)

quyet_dinh_thiet_ke:
  - >
    DỜI, không chép. `v-nap` 16819 ký tự; chép sang hai màn là +16 KB trên một
    trang đang 57/74. Dời thì tổng không đổi và `cat_khi_khac` giữ mỗi màn ở
    trang của nó — đo được: trang lớn nhất GIẢM 57 → 55
  - >
    `catNap` tổng quát thành "cắt mọi màn `cat_khi_khac`, TRỪ màn đang render".
    Bản gõ cứng `v-nap` đúng đúng một lần rồi hết hạn
  - >
    GIỮ dải tab, còn BA. Luật "không gộp chung" nói về MODULE, không nói về ba
    lựa chọn bên trong một module — và ba lối đều nhận một BẢN PHÂN TÍCH
  - >
    `nap-video` CHƯA vào bảng khai: đường đó chưa tồn tại, và T01-21 đã đặt luật
    "bảng mô tả màn CÓ THẬT". Chi tiết để thêm ghi sẵn trong `$comment`

kiem_hai_chieu:
  - "R5 ĐỎ TRƯỚC có thật: 17 lỗi trên code chưa có hai màn"
  - "dán ô chọn hiện vật vào màn Bài viết ⇒ đỏ 'lối tài liệu vẫn nằm chung'"
  - "bỏ `cat_khi_khac` của màn tài liệu ⇒ đỏ 'bị cắt khỏi trang khác'"
  - "cả hai hoàn tác khớp sha256"

tu_bat_duoc:
  - >
    HỎNG THẬT: bỏ dải tab làm lối `file` và `viet` (mang `hidden`) không còn gì
    bật hiện — HAI TRONG BA lối của màn Bài viết biến mất. Cổng bắt được, đã
    khôi phục dải tab ba lối
  - >
    `#mbnap` thành ID TRÙNG vì khung đầu bị nhân đôi. `chen()` thay theo id nên
    hai thẻ cùng id là một phép thay không xác định. Màn tài liệu giờ có
    `#mbnaptl` riêng, và dải của nó nói đúng việc máy làm cho MỘT FILE
  - >
    Một `</div>` LẠC làm `.mid` đóng sớm ⇒ màn tài liệu nằm ngoài khung
  - >
    ĐO SAI khi đi truy: lát cắt tới `</main>` nuốt luôn thẻ đóng `.mid` nên khối
    tài liệu báo -1 trong khi nó đúng. Cộng một lượt `assert` ném TRƯỚC
    `write_bytes` nên bản vá chưa từng được áp — mà tôi đã đọc kết quả như thể
    nó đã áp
  - >
    `MAN_LOAI` lọc `m.module` (viết ở C5) sai từ C6a: hai màn NẠP cũng khai
    `module` nên chúng lọt vào vòng đổ lưới và `m.nhan` là `null` ⇒ vỡ tại
    `.toLowerCase()`. Siết thành `m.menu && m.module` ở CẢ trang.mjs lẫn cổng
  - >
    Bốn kỳ vọng hết hạn ở bốn file test, và một trong đó (`chu-giao-dien`) CHẾT
    bằng TypeError thay vì đỏ một phép kiểm
  - "Sự cố escaping thứ 17-18 qua heredoc; hai lượt cuối chuyển sang tool Edit"

do_do:
  - cong: core/tests/check_skill.py
    do_do_boi: agent khác
  - cong: check_danh_muc · check_frozen · check_running
    do_do_boi: chính tôi, từ FR-039 — hai ô backlog đang mở, chờ người dùng
    quy_chu: y hệt WL-01K9NGFR039, không phải đỏ mới

con_lai:
  - "C6b · `/video/nap/` — đường TẠO video chưa tồn tại. Thêm `nap-video` vào bảng khai cùng lúc với màn; FE gọi `/api/video` đã có sẵn từ FR-040"
  - "C7 · CRUD tại chỗ; form Tài liệu phải có ô `media`"
  - "C8 · BUG-1 `[hidden]` thua `.api-only` · BUG-2 `#acount` đếm cả trang"
