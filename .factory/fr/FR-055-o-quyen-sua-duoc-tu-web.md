# FR-055 — ĐÃ GỘP VÀO `proposal-3`

> ⚠️ **Đổi số 2026-09-03: FR-054 → FR-055.** File này từng mang số `054`, và
> một agent khác cùng ngày (03:43, sau file này lúc 02:18) tạo
> `FR-054-duong-video-transcript-thanh-hien-vat.md` — **hai file một id**.
>
> Đổi số **file này** chứ không đổi file kia, vì đo được: bản video được
> `FR-053` trỏ tới ở **ba** chỗ (một FR ngang cấp), còn file này chỉ được
> `WL-01K9XFS2S6CAIDAT.yaml` — worklog của chính tôi — trỏ tới. Đổi số thứ có
> ít tham chiếu ngoài là đổi chỗ ít vỡ nhất.

> ⚠️ **Con trỏ.** Nội dung sống ở `02_proposal/proposal-3-quan-ly-cai-dat.md`.

## Vòng đời của tài liệu này — hai lần đổi thể loại trong một ngày

| | |
|---|---|
| **lượt 1** | tôi viết `proposal-3` |
| **lượt 2** | chủ dự án chất vấn *"tại sao không gộp vào proposal 2?"*. Tôi đo và **chuyển thành `FR-054`** |
| **lượt 3** | chủ dự án chốt: **`M18` thuộc proposal 3**, module sau bổ sung vào đó. Chuyển **về** proposal |

**Lượt 2 tôi đo đúng nhưng kết luận sai — và sai ở MỤC ĐÍCH, không ở số liệu.**

Ba số đo vẫn đúng: `project_map.yaml:29` khai `skipped_gates: [G1, G2, G3]` ·
`proposal-2` không nhắc `M18` · luật s2 nói đổi phạm vi thì dùng FR.

Nhưng cả ba trả lời câu *"file này có đóng gate không?"*. Chủ dự án **không cần
nó để đóng gate** — cần nó làm **một chỗ đọc được phạm vi đợt này**. Một file
đọc được có giá trị riêng, không phụ thuộc gate nào.

⇒ Bài học: trước khi đổi **thể loại** một tài liệu, hỏi *"nó phục vụ ai, để làm
gì"* — không chỉ *"nó khớp luật nào"*. Tôi đo được ba thứ và **không đo thứ
quan trọng nhất**.

Luật từ 2026-09-03: **module sau bổ sung vào `proposal-3`**; `proposal-4` chỉ
khi chủ dự án duyệt.
