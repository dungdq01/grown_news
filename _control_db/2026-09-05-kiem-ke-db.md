# Kiểm kê DB — trạng thái & dữ liệu, 2026-09-05

**Đo lúc**: 2026-09-05 · **agent**: kiểm soát dữ liệu · chỉ báo cáo.
**Phạm vi**: mọi file `.sqlite` trên đĩa + hai thư mục bản lùi.

> ⚠️ **Khai trước — tôi đã chạy một lệnh GHI.**
> Để kiểm điểm bất động tôi chạy `core/tools/dung_lai_db.py`, và lệnh đó **xoá
> `kb/_kho.sqlite` rồi dựng lại từ `kb/`**. Đo lại sau khi chạy: **11/11 bảng
> cùng số hàng**, `PRAGMA integrity_check` = `ok`, hash `86dab76b3e68a520`.
> **Không mất dữ liệu.** Nhưng đây là một lệnh ghi mà vai của tôi không được
> phép — nếu team M12 đang có dữ liệu trong DB chưa xuất ra file, tôi đã xoá
> nó. Ghi ra để không lặp lại. *(Team M12 đang thi công s7/s8 — `git status`
> cho thấy `kb/` có 6 file sửa + 2 file mới, nhưng đó là thay đổi có sẵn, không
> phải do lệnh này.)*

---

## 1 · Bản đồ — cái gì nằm ở đâu

```text
┌─ Ổ ĐĨA ────────────────────────────────────────────────────────────────────┐
│                                                                            │
│  ┌── VÙNG LÕI · dữ liệu GỐC ──────────┐   ┌── VÙNG KHO · dẫn xuất ───────┐  │
│  │                                    │   │                              │  │
│  │  web/_loi.sqlite       116 KB      │   │  kb/**/*.md      6 file      │  │
│  │  ┌──────────────────────────────┐  │   │  kb/*.yaml       3 file      │  │
│  │  │ nguoi_dung          0 hàng   │  │   │  kb/_media/      2 file      │  │
│  │  │ ma_moi              0 hàng   │  │   │  kb/_audit.jsonl             │  │
│  │  │ dinh_danh_kenh      0 hàng   │  │   │           │                  │  │
│  │  │ phien               0 hàng   │  │   │           │ dung_lai_db.py   │  │
│  │  │ nhap_chung_cat     23 hàng ◄─┼──┼───┼───────┐   ▼  (file → DB)     │  │
│  │  │ ─────────────────────────    │  │   │       │ kb/_kho.sqlite 3,0MB │  │
│  │  │ audit_loi          49 hàng   │  │   │       │ ┌──────────────────┐ │  │
│  │  │ lan_thu            23 hàng   │  │   │       │ │ bai_viet    2    │ │  │
│  │  │  ▲ HAI BẢNG NGOÀI DDL        │  │   │       │ │ tai_lieu    2    │ │  │
│  │  └──┼───────────────────────────┘  │   │       │ │ video       1    │ │  │
│  │     │                              │   │       │ │ media       2    │ │  │
│  │     │ xuatLoi()  (DB → file)       │   │       │ │ concepts    2    │ │  │
│  │     ▼                              │   │       │ │ categories  3    │ │  │
│  │  _backup/   4 file · 62 KB         │   │       │ │ loai_nguon 14    │ │  │
│  │   ├ nguoi-dung.yaml      [rỗng]    │   │       │ │ audit_log   8    │ │  │
│  │   ├ dinh-danh-kenh.yaml  [rỗng]    │   │  xuat_kho.py │ meta      4  │ │  │
│  │   ├ nhap-chung-cat.yaml  54,9 KB   │   │  (DB → file)◄┤ +versions 0  │ │  │
│  │   └ audit-loi.yaml        7,7 KB   │   │       └──────┤ +recycle  0  │ │  │
│  │  ⛔ GITIGNORE (FR-050 cách 2)      │   │              └──────────────┘ │  │
│  └────────────────────────────────────┘   │  ⛔ GITIGNORE (FR-034)        │  │
│                                           └──────────────────────────────┘  │
│  ┌── MỒ CÔI ────────────────────────────────────────────────────────────┐   │
│  │  kb/_index.sqlite   52 KB · sửa lần cuối 2026-08-26 · RETIRED FR-034 │   │
│  │  web/_luu/          3 file · 2026-09-02 · thay bởi _backup/ FR-050   │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────────────┘
```

**Đọc bản đồ**: mũi tên là đường dữ liệu duy nhất được phép giữa hai bên.
`kb/_kho.sqlite` **dựng lại được** — mất nó không mất gì. `web/_loi.sqlite`
**không** — mất nó là mất 23 bản nháp + 49 dòng audit, và bản thứ hai duy nhất
là `_backup/` trên cùng một ổ.

---

## 2 · Thống kê từng bảng

### 2.1 · `kb/_kho.sqlite` — 3.137.536 byte · **14/14 khớp DDL** ✅

| bảng / view | hàng | nội dung thật |
|---|---:|---|
| `bai_viet` | **2** | `article/phan-tich-linux-foundation` **draft** · `docs/xgboost-taylor-bac-hai` **approved** |
| `tai_lieu` | **2** | `linux-foundation` · `xgboost-stap-by-step` — cả hai **approved** |
| `video` | **1** | `thien-duong-chuot-…` **approved** |
| `media` | **2** | `6714ee19…` 2.976.936 B (pdf) · `cd314279…` 3.793 B (md) |
| `concepts` | **2** | `talkshow` · `algorithm` |
| `categories` | **3** | `aitalkshow` · `ml` · `hcsy` |
| `loai_nguon` | **14** | bài-viết 5 · tài-liệu 6 · video 3 |
| `audit_log` | **8** | 2026-08-29 → 2026-09-04, **toàn bộ là thao tác danh mục** |
| `meta` | **4** | `concepts_header` 483 B · `categories_header` 880 B · `nhat_ky_goc` **0 B** · `schema_version` |
| `article_versions` | **0** | chưa re-analyze bài nào |
| `recycle` | **0** | thùng rác trống |
| *view* `ban_ghi` | 5 | 2 + 2 + 1 |
| *view* `nhan` | 11 | category 4 · concept 4 · proposed 3 |
| *view* `tham_chieu_media` | 5 | 5 nhánh, khai một nơi |

**Trạng thái duyệt — cả kho có 5 bản ghi:**

```
approved ████████████████████████████████████████░░░░░░░░  4 / 5   (80%)
draft    ██████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  1 / 5   (20%)
edited   ················································  0
rejected ················································  0
```

**Nhãn — ai gắn vào cái gì:**

```
category   ml         ██ 2 bản ghi
           aitalkshow █  1
           hcsy       █  1
concept    algorithm  ██ 2
           talkshow   ██ 2
proposed   taylor-bac-hai-trong-boosting   █ 1  ┐ 3 khái niệm ĐỀ XUẤT
           hessian-lam-do-cong             █ 1  │ chưa ai duyệt vào
           dieu-chuan-trong-ham-muc-tieu   █ 1  ┘ danh mục kiểm soát
```

**Hiện vật**: 2 object, tổng **2.980.729 byte** (2,84 MB) — khớp 1-1 với
`kb/_media/`. `la_dan_xuat = 0` cả hai · `kieu_moc = NULL` cả hai *(cột mới của
`FR-054`, chưa có dữ liệu)*.

### 2.2 · `web/_loi.sqlite` — 118.784 byte · **7 bảng thật / 5 khai** ❌

| bảng | hàng | khai trong `loi.schema.sql`? | trong `XUAT_LOI`? |
|---|---:|---|---|
| `nguoi_dung` | **0** | ✅ | ✅ *(file rỗng)* |
| `ma_moi` | **0** | ✅ | ⛔ cố ý — bí mật |
| `dinh_danh_kenh` | **0** | ✅ | ✅ *(file rỗng)* |
| `phien` | **0** | ✅ | ⛔ cố ý — session |
| `nhap_chung_cat` | **23** | ✅ | ✅ 54,9 KB |
| **`audit_loi`** | **49** | ❌ **KHÔNG** | ✅ 7,7 KB *(mới — xem §3.1)* |
| **`lan_thu`** | **23** | ❌ **KHÔNG** | ⛔ không |

**Hàng đợi nháp chưng cất — 23 job:**

```
nhap      █████████████████████  21   (91%)  chờ người xem
da_duyet  █                       1   ( 4%)  đã vào kho
tra_lai   █                       1   ( 4%)  trả về, có lý do
                                 ──
                                  23   từ 2026-09-04 04:17 → 2026-09-05 09:25

review_status:  draft  23/23   (CHECK ép cứng = 'draft')
nguoi_dung_id:  NULL   23/23   ← MỌI job đều KHÔNG CHỦ
đã người sửa:   0/23           ← ban_goc_ai == ban_hien_tai, không một bản nào
```

**Audit của LÕI — 49 dòng, và cả 49 đều `ok = 1`:**

```
tao-job-khong-chu     ████████████████████████  24
tao-nhap-chung-cat    ███████████████████████   23
duyet-nhap-vao-kho    █                          1
tra-lai-nhap          █                          1
                                                ──
thất bại (ok = 0)                                 0   ← chưa từng có dòng nào
```

**Chặn dò — `lan_thu` 23 lượt, `ma_bam` NULL cả 23.** Nghĩa là chiều *"nhiều
máy cùng thử MỘT mã"* **chưa từng được kích hoạt** — hợp lý, vì chưa có mã mời
nào tồn tại.

### 2.3 · `kb/_index.sqlite` — 53.248 byte · **MỒ CÔI**

| bảng | hàng | ghi chú |
|---|---:|---|
| `analyses` | 2 | ảnh chụp kho lúc **2026-08-26** |
| `nhan` | 5 | — |

`FR-034` khai file này **RETIRED**, và `.gitignore` ghi *"giữ ignore một nhịp để
file cũ còn nằm trên đĩa không làm bẩn git; **xoá hẳn ở bước dọn C7**"*.
Bước dọn đó chưa chạy. Không code nào đọc nó *(đã kiểm)*, nên nó vô hại — nhưng
nó là một DB **nói sai về kho** đang nằm cạnh DB nói đúng.

---

## 3 · Trạng thái — sáu điều đáng nói

### 3.1 · ✅ ĐÃ ĐÓNG — audit của LÕI nay có bản lùi

Báo cáo 09-03 mục `A3` khai: *"`audit_loi` không nằm trong `XUAT_LOI` ⇒ mất
`web/_loi.sqlite` là mất toàn bộ audit tài khoản."*

**Đã sửa.** `dungchung.mjs:1722` thêm `{ bang: "audit_loi", file: "audit-loi.yaml" }`,
và `_backup/audit-loi.yaml` **có thật, 7.679 byte, 49 dòng**. Biện pháp bù của
`ADR-06 (c)` — *"dấu vết của `ma_moi`/`phien` sống trong audit"* — nay tồn tại.

### 3.2 · `[chặn]` VẪN MỞ — hai bảng ngoài DDL

`audit_loi` và `lan_thu` vẫn được tạo bằng `CREATE TABLE IF NOT EXISTS` **trong
thân hàm** (`dungchung.mjs`), không trong `web/api/loi.schema.sql`.

```bash
# khai 5 | thật 7
# CÓ TRONG DB MÀ KHÔNG KHAI: ['audit_loi', 'lan_thu']
```

Hệ quả đã đo ở báo cáo 09-03 và **chưa đổi**: ba hàm tạo `audit_loi` với **hai
hình dạng khác nhau**; hàm nào chạy trước trên DB mới thì hình dạng đó thắng
vĩnh viễn, và bản thiếu cột `boi` làm mọi phép ghi audit sau đó **ném lỗi**.

`BANG_LOI` (`loidb.mjs:37`) vẫn liệt **5** và vẫn **không cổng nào gọi nó**.

### 3.3 · `[chặn]` `check_db_dung_cho` ĐỎ — **và đây là ĐỎ OAN**, lần thứ ba

```
FAIL: `ma_moi` KHONG duoc xuat trang thai ra file (ADR-06 c) nhung mot exporter co khai
FAIL: `phien`  KHONG duoc xuat trang thai ra file (ADR-06 c) nhung mot exporter co khai
```

**Quy chủ được, và không phải một vi phạm thật.** Cổng khớp `\bma_moi\b` /
`\bphien\b` trong khối `XUAT_LOI`, và thứ nó bắt được là **một dòng chú thích**:

```js
// dungchung.mjs — dòng thứ 35 của khối XUAT_LOI
// `ma_moi` + `phien` VẪN không xuất — ADR-06 (c) giữ nguyên vế bí mật.
```

Bằng chứng nó không phải vi phạm thật:

| | đo |
|---|---|
| `_backup/` có file `ma-moi.yaml` / `phien.yaml`? | **không** — đúng 4 file, không cái nào |
| `XUAT_LOI` có mục nào cho hai bảng đó? | **không** — 4 mục: `nguoi_dung`, `dinh_danh_kenh`, `nhap_chung_cat`, `audit_loi` |
| grep bí mật trong `_backup/*.yaml` | **0 file** khớp `ma_moi\|het_han\|ngu_canh\|dung_luc` |
| hàng thật trong hai bảng | `ma_moi` 0 · `phien` 0 |

⚠️ **Đây là lần thứ BA của cùng một lớp lỗi trong dự án này** — một câu chú
thích khẳng định *đúng điều luật đòi* lại làm cổng đỏ:

1. `loi-cua.mjs:1229-1231` — *"Bình luận này CỐ Ý không viết câu lệnh INSERT ra
   chữ… cổng đỏ vì một BÌNH LUẬN"*;
2. `cac-man-con-lai.test.js:117` — *"thứ phá phép cắt là một literal nằm trong
   chú thích"*;
3. **hôm nay**, ở chính cổng canh chỗ-ở-dữ-liệu.

Trớ trêu: `check_db_dung_cho.py` đã **tự sửa một lần** đúng lỗi này (*"Luot dau
toi grep ten bang tren TOAN BO noi dung hai file va no DO OAN"*) — và bản sửa
thu hẹp phạm vi xuống khối `XUAT_LOI` nhưng **vẫn không bỏ chú thích**.

**Đề xuất (người quyết)**: cổng đọc khối `XUAT_LOI` **sau khi bỏ comment** —
`dungchung.mjs` đã có sẵn hàm `boComment()` mà `api-guard.test.js:28` dùng cho
đúng việc này, kèm câu giải thích *"So code, không so lời kể"*.

### 3.4 · `[hỏi]` 23/23 bản nháp **không có chủ**

`audit_loi` ghi `tao-job-khong-chu` **24 lần** — nhiều hơn cả số job. Và
`nguoi_dung` có **0 hàng**, nên `nhap_chung_cat.nguoi_dung_id` là `NULL` toàn bộ.

`FR-047 L1` đòi *"danh tính do LÕI gán, KHÔNG nhận từ payload"* — và LÕI đang
gán `NULL`, đúng theo mã (`loi-cua.mjs:219`: `b.phien ? … : null`).

Câu hỏi: đây là **chế độ bootstrap có chủ ý** (chưa mời ai nên chưa ai có chủ),
hay là **một lỗ sẽ ở lại** sau khi có tài khoản? `FR-047 L2` khai *"thiếu danh
tính ⇒ **từ chối**, không nới"* — nhưng `C2` không từ chối, nó ghi `NULL`.
Hai câu này không cùng nói một điều.

### 3.5 · `[hỏi]` `ban_goc_ai == ban_hien_tai` cho **cả 23** bản

`FR-046` tồn tại để nút *"khác gì bản AI"* diff được hai cột. Sau 23 job và
2 thao tác duyệt/trả-lại, **chưa bản nào từng bị người sửa**.

Không phải lỗi — nhưng nghĩa là **đường sửa chưa có bằng chứng chạy thật**, và
trigger `nhap_chung_cat_ban_goc_bat_bien` chưa từng có cơ hội bắn. Cùng hình
dạng *"cổng xanh vì chưa tới được chỗ cần đo"* mà `FR-047 §6b` đã tự khai.

### 3.6 · `[nên]` Hai thư mục bản lùi, một cái chết

| | sửa lần cuối | ai đọc |
|---|---|---|
| `_backup/` | **2026-09-05 16:25** | `LUU_LOI()` — đường thật |
| `web/_luu/` | 2026-09-02 | **không ai** — chỉ còn một câu chú thích ở `web/test/_api.mjs:314` |

`FR-050` cách 2 chuyển đích sang `_backup/`. `web/_luu/` còn 3 file cũ 3 ngày,
nội dung **khác** `_backup/` *(1.175 B vs 160 B cho `nguoi-dung.yaml`)*. Một
người đi tìm bản lùi có thể mở nhầm cái chết — và cái chết trông **giống một
bản lùi có dữ liệu hơn**, vì nó to hơn.

---

## 4 · Cổng — 18 xanh / 11 đỏ, và ai là chủ

```bash
for f in core/tests/check_*.py; do PYTHONIOENCODING=utf-8 py -3.13 "$f"; done
# xanh=18  do=11  tong=29
```

| cổng đỏ | quy chủ |
|---|---|
| `check_db_dung_cho` | **ĐỎ OAN** — §3.3, chú thích trong `XUAT_LOI` |
| `check_media_ddl` | *"trần hiện vật 1024 MB · mọi mime khai `magic`"* — **2 ràng buộc DDL không bắn thật** sau lần ký 1 GiB (`FR-054-va-tran-1gb`) |
| `check_danh_muc` | `categories.yaml` — hai bản schema lệch (`core/skill-src` vs `core/assets`) |
| `check_g6b` · `check_worklog` · `check_dinh_dang_mo` · `check_khai_mot_noi` · `check_loai_nguon_db` · `check_rule_surfaces` · `check_running` · `check_skill` | **team M12 đang thi công s7/s8** — nêu đích danh `T12-20`, `tab-theo-doi-chung-cat`, màn hình, loại nguồn. **Không phải việc của tôi phán**; ghi ra để đếm được, không để quy trách |

⚠️ `check_media_ddl` **đáng nhìn**: nó nói lần ký trần 1 GiB đã đổi con số nhưng
**hai ràng buộc DDL không cưỡng chế nó** — tức trần mới là một con số trong
schema mà engine không bắt. Đây là DB, nên nó thuộc phạm vi báo cáo này; ba cổng
kia thuộc M12.

---

## 5 · Vòng đời một bản ghi — nơi 23 job đang đứng

```text
   THỢ · chungcat                LÕI · web                       KHO
   ─────────────────             ─────────                       ───

   model sinh nháp
        │
        │ C2  POST /api/nhap-chung-cat
        ▼
   ┌─────────────────────────────────────┐
   │  nhap_chung_cat.trang_thai = 'nhap' │  ◄── 21 job ĐANG Ở ĐÂY
   │  ban_goc_ai  (bất biến, trigger)    │
   │  ban_hien_tai                       │
   │  review_status = 'draft' (CHECK)    │
   └──────────┬──────────────┬───────────┘
              │              │
     người sửa│              │ người xem
              ▼              │
      ban_hien_tai đổi       │        ◄── 0/23 job từng đi đường này
      (goc GIỮ NGUYÊN)       │
              │              │
      ┌───────┴──────────────┴────────┐
      ▼                               ▼
 ┌──────────┐                   ┌──────────┐
 │ tra_lai  │ 1 job             │ da_duyet │ 1 job
 │ + ly_do  │                   └────┬─────┘
 └──────────┘                        │ validate.py --strict
                                     ▼
                            ┌────────────────────┐
                            │ kb/<loai>/<slug>.md│ ← file là chân lý từ đây
                            │ + dung_lai_db      │
                            └────────┬───────────┘
                                     ▼
                            ┌────────────────────┐
                            │ bai_viet/tai_lieu/ │  5 bản ghi
                            │ video (kho.sqlite) │  4 approved · 1 draft
                            └────────────────────┘
```

**Đọc sơ đồ**: đường bên trái (*người sửa nháp*) **chưa ai đi**. Cả 23 bản nháp
giữ nguyên chữ của model. Nút *"khác gì bản AI"* — lý do `FR-046` tồn tại —
hôm nay luôn trả lời *"không khác gì"*.

---

## 6 · Tóm tắt một màn hình

```text
┌── DỮ LIỆU CÓ THẬT, 2026-09-05 ────────────────────────────────────────────┐
│                                                                           │
│  KHO           5 bản ghi   ·  4 approved · 1 draft  ·  2,84 MB hiện vật   │
│                2 khái niệm ·  3 chủ đề  ·  3 đề xuất chưa duyệt           │
│                                                                           │
│  TÀI KHOẢN     0 người     ·  0 mã mời  ·  0 định danh kênh · 0 phiên     │
│                └─ hệ CHƯA MỜI AI. Bản lùi có, nhưng rỗng.                 │
│                                                                           │
│  HÀNG ĐỢI     23 nháp      ·  21 chờ · 1 duyệt · 1 trả lại                │
│                └─ 0 bản từng được người sửa  ·  23/23 không có chủ        │
│                                                                           │
│  DẤU VẾT      49 dòng audit LÕI (0 thất bại)  ·  8 dòng audit kho         │
│               23 lượt chặn-dò ghi nhận (0 lượt theo mã)                   │
│                                                                           │
│  SỨC KHOẺ     kho    dựng lại được ✅  hash 86dab76b3e68a520              │
│               LÕI    7 bảng thật / 5 khai  ❌                             │
│               cổng   18 xanh / 11 đỏ  (1 đỏ oan · 8 của team M12)         │
│               rác    kb/_index.sqlite · web/_luu/  — hai thứ đã chết      │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## 7 · Đề nghị — người quyết

| # | việc | vì sao bây giờ |
|---|---|---|
| 1 | Sửa `check_db_dung_cho` bỏ comment trước khi grep *(§3.3)* | một cổng đỏ oan là một cổng sẽ bị bỏ qua, và cổng này canh chỗ-ở của **bí mật** |
| 2 | Đưa `audit_loi` + `lan_thu` vào `loi.schema.sql`, cắm cổng dùng `BANG_LOI` *(§3.2)* | mở từ 09-03; và bảng audit vẫn có đường mất cột `boi` vĩnh viễn |
| 3 | Trả lời §3.4 — `nguoi_dung_id = NULL` là bootstrap hay là lỗ? | trước khi mời người đầu tiên, không phải sau |
| 4 | `check_media_ddl` — hai ràng buộc trần 1 GiB không bắn thật | trần vừa được người ký; một trần không cưỡng chế được là một trần không tồn tại |
| 5 | Dọn `kb/_index.sqlite` (bước C7 của `FR-034`) và `web/_luu/` | hai thứ đã chết, một trong hai trông giống bản lùi thật |

**Không đề nghị** đụng gì thuộc M12 — team đang thi công, và 8 cổng đỏ kia là
biên ghi của họ.

---

*Agent kiểm soát dữ liệu · chỉ báo cáo · không ký gate · không mở FR.*
